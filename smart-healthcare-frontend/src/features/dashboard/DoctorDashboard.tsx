import {
  Box,
  Typography,
} from '@mui/material';

export default function DoctorDashboard() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Doctor Dashboard</Typography>
    </Box>
  );
}