import {
  Box,
  Typography,
} from '@mui/material';

export default function AdminDashboardPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Admin Dashboard</Typography>
    </Box>
  );
}