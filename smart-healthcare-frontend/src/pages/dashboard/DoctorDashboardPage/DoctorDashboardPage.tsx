import {
  Box,
  Typography,
} from '@mui/material';

export default function DoctorDashboardPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Doctor Dashboard</Typography>
    </Box>
  );
}