import {
  Box,
  Typography,
} from '@mui/material';

export default function PatientDashboardPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Patient Dashboard</Typography>
    </Box>
  );
}